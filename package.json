{
  "name": "@kinday/browser-homepage",
  "private": true,
  "description": "A page to be used when empty tab is open",
  "scripts": {
    "build": "parcel build index.html",
    "start": "parcel index.html --open"
  },
  "dependencies": {
    "@parcel/service-worker": "^2.8.0",
    "parcel": "^2.8.0"
  },
  "packageManager": "pnpm@7.16.1"
}
